{
  "dependencies": {
    "react": "^16.8.4",
    "react-router": "^4.3.1",
    "react-router-dom": "^4.3.1"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ..",
    "deploy": "cp -a client/build/ . public/",
    "postinstall": "npm run build && npm run deploy && echo 'App built!'"
  }
}
